# --- Core framework -----------------------------------------------------
Django==5.1.9
python-decouple==3.8
psycopg2-binary==2.9.10     # only needed when DB_ENGINE=postgres (use psycopg2 not -binary in real production)
mysqlclient==2.2.7          # only needed when DB_ENGINE=mysql

# --- Forms / UI -----------------------------------------------------------
django-crispy-forms==2.4
crispy-bootstrap5==2025.4

# --- Background jobs (scheduled publishing) --------------------------------
celery==5.5.2
django-celery-beat==2.7.0
redis==5.0.4

# --- Content generation (AI captions / images) -----------------------------
openai==1.82.1
Pillow==11.2.1
diffusers==0.33.1           # local Stable Diffusion image generation (optional/heavy)
torch==2.7.1                # required by diffusers; CPU-only wheel recommended for most deployments
transformers==4.52.4
accelerate==1.7.0

# --- Reporting / exports -----------------------------------------------------
openpyxl==3.1.5
xhtml2pdf==0.2.16

# --- Production server ------------------------------------------------------
gunicorn==22.0.0
whitenoise==6.7.0
